target
node_modules
Cargo.lock
!.gitkeep
.freeCodeCamp/dist

# Should be included in repo, but does not get updated
test-ledger

# Untracked files that should not be removed when lesson is reset
*.testnet*
